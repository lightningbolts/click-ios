# Parity Ledger

Tracks implementation, testing, and parity status of every reachable Click flow.

"Automated" records only tests that actually exist in this repository. "Device-Tested" remains Pending until the flow has been exercised on physical hardware or an in-place TestFlight update as appropriate. A green CI build does not count as device verification.

| ID | Flow | Backend Route | Native Entry | States Implemented | Automated | Device-Tested | Classification | Notes |
|---|---|---|---|---|---|---|---|---|
| F01 | Legacy Session & State Migration | N/A (Keychain & UserDefaults) | `LegacyKMPStateMigrator`, `KeychainSessionVault` | Decode, missing/invalid record, JWT sub derivation, retired-key cleanup, non-destructive session replacement | Partial (unit fixtures) | Pending | P0 COMPATIBILITY | In-place KMP -> Swift Keychain compatibility still requires physical update-install verification; later queue/cache domains remain preserved for phase-specific migration. |
| F02 | Settings Preference Migration | N/A (UserDefaults) | `SettingsStore` | Legacy suite reads/writes, per-user onboarding cache, session reset | Yes (unit) | Pending | P0 COMPATIBILITY | Uses `click_auth_prefs`; retired call/home-layout behavior is not restored. |
| F03 | Root Gate & App Launch | Self profile + auth | `RootGateView`, `AppEnvironment` | Restoring, Auth, Profile Basics, onboarding resolution, retryable onboarding-load failure, Main | Partial | Pending | P0 PARITY | Full cold/warm/offline/update gate sequence needs integration/device coverage. |
| F04 | Typed Routing & Deep Links | N/A | `AppRouter` | Per-tab paths, pending intent, connection invocation, event/hub routes | Yes (unit) | Pending | P0 PARITY | `/c/*` routes to Add Click and preserves `token/qr_token/qt`, expiry/issued-at seconds or ms, and `venue_id`. |
| F05 | API Client Foundation | `/api/*` | `ClickAPIClient` | Bearer injection, 401 refresh, exactly-one retry, HTTP error taxonomy | Yes (unit) | Pending | P0 PARITY | Single-flight behavior is provided by `SessionController.refreshSession`; multi-request concurrency still merits an integration test. |
| F06 | App Clip Shell | `/c/{uuid}` | `ClickClip` | Target/shell only | Pending | Pending | P0 COMPATIBILITY | Full invocation/redeem path is later work. |
| F07 | Notification Service Extension | APNs payload | `NotificationService` | Category inspection, legacy v1 preview decrypt, privacy-safe fallback for v2/unknown payloads | Pending | Pending | P0 COMPATIBILITY | V2 preview decryption is intentionally not claimed until extension-safe epoch-key access is implemented and device-tested. |
| F08 | Supabase Auth Service | Supabase Auth `/auth/v1/*` | `SupabaseAuthService`, `AuthView` | Email/password, verification-required signup, Apple ID-token exchange, Google OAuth callback session, refresh | Partial | Pending | P0 PARITY | Password rules are tested; provider exchanges require staging/device verification. |
| F09 | Session Controller & Restore Policy | Supabase Auth + self profile | `SessionController` | Restore, freshness check, refresh, offline identity, hard-auth eviction, profile gate, sign out | Partial | Pending | P0 PARITY | Critical state transitions are implemented but still need dedicated mocked integration tests and physical update-install validation. |
| F10 | Profile Basics Gate & Sync | `GET/PATCH /api/users/{userId}/profile` | `ProfileBasicsGateView`, `SessionController` | Server-derived gate, 13+ validation, durable PATCH | Partial | Pending | P0 PARITY | Endpoint contract verified against click-web; device/staging round-trip not yet verified. |
| F11 | Design System Brand Alignment | N/A | `Typography.swift`, `Colors.swift`, `ClickLogo` | Manrope hierarchy, Click purple semantic palette, canonical mark | Partial (font load) | Pending | P1 DESIGN | Visual/dark-mode/Dynamic Type review still pending. |
| F12 | Permission Coordinator | iOS frameworks | `PermissionCoordinator` | Camera, Photos, Contacts, Microphone, Location, Calendar, Bluetooth state, Notifications, Settings handoff | Pending | Pending | P0 PARITY | Contextual API exists; each OS permission path still needs device verification. |
| F13 | Native Camera & Avatar Upload | `POST /api/user/avatar` | `AvatarService`, `NativeCameraPicker` | Capture/library, normalize/downsample, JPEG <=2MB, upload | Partial (image processing) | Pending | P0 PARITY | Real camera + authenticated upload must be tested on device. |
| F14 | Privacy Contact Discovery | `POST /api/contacts/discover`, `POST /api/connections/prior/request` | `ContactDiscoveryService`, `PriorConnectionsView` | Phone/email normalization, SHA-256, match display, known-since, request | Partial (normalization/hash) | Pending | P0 PARITY | Privacy disclosure now matches behavior; real Contacts permission and backend matching require device/staging validation. |
| F15 | Onboarding Flow & Server Reconciliation | `GET/PATCH /api/users/{userId}/profile` | `OnboardingRepository`, `OnboardingCoordinator` | Welcome, Interests, Personality, Avatar, Prior Connections, loading/error/retry, returning-user reconciliation | Partial (state-machine/unit) | Pending | P0 PARITY | Server saves are implemented; full returning-user/new-user flows still need staging/device verification. |
| F16 | End-to-End Encryption (V1 & V2) | `/api/chat/devices`, `/api/chat/epochs` | `ClickCryptoV1`, `ClickCryptoV2`, `DeviceIdentityVault`, `ChatRepository` | V1 legacy decrypt/write; V2 device discovery/registration, epoch discovery/create/rotate, recipient unwrap, envelope encrypt/decrypt, fail-closed writes | Yes (primitive unit coverage) | Pending | P0 PARITY | Crypto primitives are covered; Android/web cross-client fixtures, existing-device historical-key transfer, and physical update-install compatibility are still merge/release gates. |
| F17 | Direct Chat Messaging API | `/api/chat/messages`, `/api/chat/reactions` | `ChatRepository`, `ConversationModel` | Canonical chat resolution, fetch/send, encrypted edits, soft delete, persisted reactions, read/delivery, rollback-safe optimistic mutations | Yes (model unit coverage) | Pending | P0 PARITY | Plaintext fallback was removed. Live backend roundtrip and cross-client crypto compatibility remain unverified. |
| F18 | Realtime Message Streaming & Presence | Supabase Realtime WebSocket | `ChatRealtimeManager` | Phoenix join/heartbeat/reconnect, postgres insert/update/delete, typing broadcast/decay, decrypted realtime ingestion | Partial | Pending | P0 PARITY | Requires staging socket verification with token refresh/rebind and two real clients; no simulator-only claim is treated as transport verification. |
| F19 | Direct Chat UI & Interactions | N/A (SwiftUI Interface) | `ChatView`, `ConversationModel`, `MessageBubbleView`, `ChatComposerView` | Native push/pop and interactive back, hidden tab chrome in chat, date separators, receipts, direction-locked swipe reply, context menu/reactions, reply/edit composer, interactive keyboard dismissal, near-bottom scroll ownership | Partial | Pending | P1 QUALITY / P0 PARITY | Preview screenshots are not treated as quality verification. Physical-device gesture, keyboard, frame-pacing, VoiceOver, and rapid-scroll validation remain required. |
| F20 | Standard APNs Registration & Routing | `POST /api/user/push-tokens` + APNs payload | `ClickAppDelegate`, `ClickNotificationCoordinator` | Existing-permission re-registration, secure pre-auth token queue, standard-token upload, user-scope protection, typed chat/event/hub/connection routing | Partial | Pending | P0 PARITY | Permission is not prompted on launch. APNs token rotation, notification tap routing, and foreground behavior require device verification. |
| F21 | Home (linear feed) | `/api/me/recap`, `/api/me/nudges` (+`/{id}/dismiss`, `/{id}/acted`), `/api/user/availability-intents`, `/api/me/event-bookmarks`, `/api/beacons`, `/api/hub/nearby`, inbox via shell `ConversationListModel` | `HomeView`, `HomeFeedModel`, `AvailabilitySheet` | Prototype hierarchy (greeting/search → I'm down for… → one social opportunity → recent people strip + secondary nudge → recap Day/Week → saved & upcoming → nearby counts → insights); per-module cached/loading/fresh/confirmed-empty/failed/unavailable states; no fake-zero recap; discovery unavailable without existing location permission (never prompts); opportunity priority (live/today saved event → live nearby event → shared-event nudge → <12h say-hi → reconnect nudge → nearby today); nudge dismiss/acted with cache pruning | Yes (unit: opportunity priority, module states, recap decode, beacon/bookmark decode, card-visual golden parity) | Pending | P0 PARITY | Featured event now sourced from real bookmarks/nearby events; nearby counts from real `/api/beacons` + `/api/hub/nearby`. Server `/api/me/recap` returns zeros with 200 on some internal failures — that server behavior cannot be distinguished client-side. Visual/Dynamic Type/device scroll verification pending. |
| F22 | Saved Events | `GET /api/me/event-bookmarks` | `SavedEventsView` (route `.savedEvents`) | Cached seed, refresh, upcoming vs past/unavailable sections, stale notice, error w/ retry, confirmed empty, rows → canonical `.event` route | Partial (decode) | Pending | P0 PARITY | Deleted events render as unavailable (not tappable). |
| F23 | Deterministic beacon visuals | N/A (port of click-web `generateCardVisual`) | `CardVisual`, `EventVisual` | FNV-1a seed, weighted hue buckets, pattern, WCAG scrim; uploaded image override | Yes (golden fixtures from web) | Pending | P0 PARITY | Pattern ink drawn with a static `Canvas`; no scroll-time animation. |
| F24 | Me root (identity/account home) | `GET /api/users/{id}/profile`, `PATCH /api/user/availability`, `/api/user/availability-intents`, `/api/me/event-bookmarks`, `POST /api/user/avatar` | `MeView` (route `settings`, label Me) | Identity-first header (avatar edit, name, Clicks count, availability status), Core strip from real inbox data, grouped regions (Free currently · Availability post · Saved events · Calendar / Alerts · Privacy & data · Permissions · Interests · Personality / Dark mode · Web dashboard / Sign out · Delete account); Free currently pending→server echo→saved, failure alert + revert; unavailable (not "off") when profile read fails | Yes (unit: self-profile decode, free-currently echo requirement) | Pending | P0 PARITY | Me tab avatar now seeded from the same self-profile read. `SettingsStore` preferences made observable so Dark mode applies live. |
| F25 | Alerts (notification preferences) | Read `notification_preferences` under RLS (KMP contract), write `PATCH /api/user/preferences` | `AlertsSettingsView` | Message notifications, Event reminders, Reconnect nudges, Availability matches, Hub messages; per-toggle pending state; server row is truth; failed read is an error (never "all on"); OS permission denied → Settings handoff; notDetermined → request only after user intent; Ambient sound enrichment requests microphone contextually | Yes (unit: defaults vs failure, save echo) | Pending | P0 PARITY | Event teasers (Seed-a-Room) and call notifications intentionally absent. Server-side push gating is backend behavior (not verified from iOS). |
| F26 | Privacy & data | `PATCH /api/user/ghost-mode`; `users.location_*` columns via RLS PostgREST (KMP contract) | `PrivacySettingsView`, `GhostModeController` | Ghost Mode session-scoped (local immediately, server bit synced, reset every launch), Location snap / Memory Map / Business insights independent server-confirmed toggles (zero-row write = failure), Location snap requests location contextually, Barometric context local opt-in | Yes (unit: zero-row write failure, defaults off) | Pending | P0 PARITY | Ghost effects on Map/Nearby/typing are implemented in their slices (see F27+). |
| F27 | Permissions hub | iOS frameworks | `PermissionsSettingsView` | Location, Bluetooth, Microphone, Camera, Photos, Contacts, Calendar, Notifications; Allow only when not determined, otherwise Settings; refresh on foreground | No | Pending | P0 PARITY | Bluetooth has no standalone request API; it is requested by Tap to Connect. |
| F28 | Interests / Personality editing | `PATCH /api/users/{id}/profile` (`tags`, `personality_tags`) | `InterestsSettingsView`, `PersonalitySettingsView` | Starts from server truth (never an empty selection), ≥5 interests, exactly 5 traits ("Pick exactly 5 traits."), save error keeps editor open | Partial (repository shared with onboarding) | Pending | P0 PARITY | Reuses the onboarding pickers with Settings copy. |
| F29 | Calendar | EventKit (read-only) | `CalendarSettingsView` | Status, contextual request, Settings handoff, privacy copy | No | Pending | P0 PARITY | No mutual-free-time consumer exists natively yet; copy does not claim one. |
| F30 | Account deletion | Web `DELETE /api/user/delete` (cookie auth) | `MeView` → confirmation → `joinclick.co/?tab=settings` | Explains permanence; opens the web settings page that hosts the name-confirmed deletion flow; never sends the bearer token | No | Pending | P0 RELEASE | **Blocker:** no bearer-authenticated deletion endpoint exists; completion happens on web and requires a web sign-in. Sign in with Apple token revocation is not verifiable from this repo. |
| F31 | Add Click root | N/A | `AddClickView` | Tap to Connect hero with one Start action, My QR / Scan quick actions, "How it works"; no unfinished rows (retired "Join hub by venue code" has no backend contract and is not shown) | No | Pending | P0 PARITY | Hub creation is surfaced from Map (hub creation lives with map/beacon creation). |
| F32 | My QR | `GET /api/qr` | `MyClickCodeView` | Identity header, 90 s token, refresh ~10 s before expiry, code hidden the moment it expires, fast retry on failure, refresh on foreground, share URL, screen kept awake while visible | No | Pending | P0 PARITY | Single `TimelineView` for the countdown (no per-render timers). |
| F33 | Tap to Connect (Tri-Factor) | `POST /api/connections/proximity`, `GET ?pending_handshake_id`, `POST /api/connections/proximity/confirm` | `TapConnectView`, `TapConnectModel`, `BLEProximityService`, `UltrasonicService`, `ProximityCodec`, `ProximityRepository` | idle → preparing (contextual mic/Bluetooth/location) → needsPermission (mic denied / Bluetooth unauthorized / off) → sensing (BLE advertise+GATT token read, 18.5 kHz chirp+digits, progressive GPS ≤6.5 s) → submitting → connected / encounter saved (already connected) / waitingForPeer (12×2.5 s recovery, resumes on foreground) / choosingPeople (≤11 peers) → confirming / savedOffline (user-scoped queue, flushed on next launch) / failed + retry + QR fallback; cancel/background stop radios and mic immediately | Yes (unit: token normalize, synthesis timing, ultrasonic round-trip incl. repeated digits + multi-peer + noise rejection, bind-result parsing, request body) | Pending | P0 PARITY | Wire-compatible with KMP (service/characteristic UUIDs, token GATT payload, chirp+digit synthesis, request body). Native uses a chirp-synchronized Goertzel decoder because the KMP decoder's filter state is incorrect and merges repeated digits (emitted audio unchanged). Location only when Location snap is on and Ghost Mode is off. **Not yet implemented:** post-tap context tagging (§26), event recommendation (§27), encounter tether (§45), connection-flow telemetry (§71.2). Requires two/three-device hardware verification (BLE, ultrasonic, GPS, Multi-Tap) — none performed. |
| F34 | Map + Nearby | `GET /api/beacons`, `GET /api/hub/nearby`, `GET /api/beacons/{id}` (focus), dashboard bundle `map` rows (pins) | `ClickMapView`, `MapFeatureModel`, `NearbySheet` | One model for map + list: stable annotation identity (people/beacons/hubs), layers (mirrors web `MapLayerToggles` + hubs), chip filter applied to both map and list, local search, selection (beacon → detail sheet, hub → hub route, person → canonical profile), focus intents from Home/Saved/deep links (`MapFocus`), location states (not determined → "Turn on", denied → Settings, approximate/precise), live location only while visible, viewport-coalesced refetch (≥1 km), cached discovery seed, loading / error (never shown as empty) / confirmed empty; Nearby lip → medium → expanded with header-only drag (list keeps native scrolling) | Yes (unit: first-meet pin + peer collapse, geo_location precedence, layer mapping) | Pending | P0 PARITY | Custom lip container used because a system sheet would cover the native tab bar on the Map root. Not implemented: beacon/event/hub **creation** (no "+" is shown), map-friction telemetry + grass nudge (§71.1), intent-match signals in Nearby. |
| F35 | Ghost Mode effects on Map | `PATCH /api/user/ghost-mode` | `MapFeatureModel.ghostModeChanged`, toolbar toggle | Stops live location, hides own dot, pauses discovery requests (no coordinates sent), grayscale/muted map, banner copy | No | Pending | P0 PARITY | Typing-presence suppression is applied in chat (see chat rows). |
| F36 | Peer profile (canonical) | `GET /api/users/{id}/profile`, `GET /api/connections?connectionId=` (event-redacted encounters), `/api/profile/timeline` (GET/POST/PUT/DELETE), `GET /api/connections/{id}/tabs`, `POST /api/safety/report`, `POST /api/safety/block`, `DELETE /api/connections?connectionId=` | `ProfileView`, `PeerProfileModel`, `JournalEditor` | Centered identity, Message primary / Nudge secondary (nudge is a normal encrypted message), relationship line from real encounters only, common ground + personality, chip tabs Timeline / Media / Links / Files / Beacons each with independent loading/error/empty, Links extracted on-device from decrypted messages (http/https only), journal add/edit/delete via sheet (private/shared), safety menu (remove / report / block) with confirmation — UI changes only after server success | Partial (link extraction) | Pending | P0 PARITY | Encounter event fields render only when the server's per-viewer eligibility returned them. Media/Files tabs list items with dates; opening a profile media item in the viewer is not wired yet. |
| F37 | Verified group profile + management | RLS reads `group_members`/`chats`/`groups`/`messages`; RPCs `rename_clique`, `leave_clique`, `delete_clique`, `create_verified_clique`; `POST/DELETE /api/cliques/members`; `POST /api/groups/{id}/avatar`; `/api/chat/devices` + `/api/chat/epochs` | `GroupProfileView`, `GroupRepository`, `GroupMemberPickerSheet`, `ChatRepository.reconcileMembershipEpoch` | Identity (custom or generated two-member avatar), Message primary, members with Creator role, creator-only add (eligible active Clicks) / remove (confirm) / rename / photo / delete, leave for everyone; every add/remove is followed by E2EE v2 epoch reconciliation and the UI reports: rotated · still legacy (not every member has a v2 device) · **membership saved but securing failed** with a "Finish Securing Group" retry; manual creation seals the 32-byte master per member with the KMP pairwise rule, then initializes v2 when possible | Yes (unit: creation payload unwrap per KMP rule, missing-edge rejection) | Pending | P0 PARITY | Legacy (non-v2) groups cannot rotate their master on removal (protocol limitation) — surfaced honestly. Staging verification of add/remove + rotation with KMP/web members not performed. |
| F38 | Conversation kinds (direct / verified group / hub) | `/api/chat/*` (direct+group), `/api/hub/messages`, `/api/hub/messages/{id}`, `/api/hub/reactions`, `/api/hub/devices`, `/api/hub/epochs`, `/api/hub/{id}`, `/api/hub/join`, `/api/hub/{id}/participants/me`, Edge Function `verify-hub-proximity`, `GET /api/beacons/{id}/event-chat` | `ConversationKind`, `ChatRepository`, `ChatRealtimeManager` (`.chat`/`.hub`/`.inbox`), `ConversationModel`, `ChatView`, `HubChatView`, `EventChatView` | One conversation feature parameterized by kind. Groups: legacy `e2e_grp:` read/write via master unwrapped from `group_members.encrypted_group_key` (KMP rule), v2 via chat epochs with server-re-read membership; sender names per run. Hubs: v2 when upgraded (hub epochs, KMP rotation rule), plaintext only for never-upgraded hubs (KMP behavior), legacy hub keys read-only, geofence evidence only when a fix exists, real access reasons (not near / location needed / RSVP / ended), no receipts, owner rename/delete, leave (standalone). Event chat always via the resolver (200/403/404/409/410 bounded states). Ghost Mode suppresses outgoing typing. | Yes (unit: hub receipts, identity helpers, hub error mapping, resolver mapping, legacy hub key golden) | Pending | P0 PARITY | Hub timeline has no older-page cursor server-side (latest 120 only). Hub **media** is not supported on iOS (shown as unavailable). Cross-client group/hub v2 verification on staging not performed. |
| F39 | Inbox realtime | Realtime `messages` INSERT (RLS-scoped) | `ConversationListModel.startRealtime/applyInserted` | Independent inbox subscription; known direct/group rows update in place (last message, time, unread, move to top), open conversation not counted unread, unknown chat → one coalesced refresh; rebinds with a fresh token on foreground, torn down in background | Yes (unit: in-place update + unread) | Pending | P0 PARITY | Token rotation while foregrounded relies on reconnect; §35 re-auth push over an open socket is not implemented. |
| F40 | Chat media (image / voice note / file) | `POST /api/chat/media`, `POST /api/chat/attachments`, `POST /api/chat/attachments/sign`, `POST /api/chat/messages` | `ChatMedia` (`MessageMedia`, `AttachmentEnvelope`, `ChatMediaVault`), `ClickCryptoV2` media, `ClickCryptoV1` media bytes, `ChatMediaViews` (`AudioPlaybackService`, `VoiceNoteRecorder`, image/audio/file bubbles, `MediaViewer`, attachment menu) | v2 chats: `encryptMedia` + `type=media` authorization bound to the message's client ID (server gate), `media_*` metadata; legacy chats: KMP `encryptMediaBytes` with pairwise / group-master keys, files via `ccx:v1:` per-file key + plaintext SHA-256; downloads re-sign expired URLs, verify, decrypt once into a protected cache vault (cleared on sign-out); reserved image box, one fullscreen viewer (zoom/pan/share/drag-dismiss), Quick Look for files, one shared audio player (seek slider wins over swipe-to-reply, interruption/route handling), contextual mic permission, one recording session, optimistic bubbles with scoped retry | Yes (unit: **click-web v2 media vector decrypt + authorization AAD**, tamper/wrong-chat rejection, legacy bytes, base64 payload normalization, descriptors, parsing) | Pending | P0 PARITY | Plaintext never uploaded. Not implemented: Click Drop/disposable capture flow, video, profile-tab media viewer. Device verification of recording/playback/QuickLook pending. |
| F41 | Reply metadata privacy | `POST/PATCH /api/chat/messages` | `ChatRepository`, `ConversationModel.resolveReplyQuotes` | Native sends only `reply_to_id` for encrypted chats and resolves quotes from the decrypted local timeline; reads `reply_to_content` (web/KMP) and legacy `reply_to_snippet` | No | Pending | P0 RELEASE | **Cross-client issue:** web (`useChatEncryption`) and KMP write a plaintext `reply_to_content` excerpt into message metadata for E2EE chats, exposing message text to the server. Native no longer does; other clients still do. |
| F42 | Conversation background | N/A | `ChatBackground` | Stationary, deterministic low-contrast wash + static motif seeded per conversation over the neutral chat surface; no network, present on first frame, never animates on scroll | No | Pending | P1 | Seeded by connection/chat ID. Encounter-context seeding, presets, and custom images are not implemented (no backend field exists for a chosen background). |
| F43 | Canonical beacon / event detail | `GET /api/beacons/{id}`, `GET/POST/DELETE /api/beacons/{id}/rsvp`, `GET /api/beacons/{id}/engagement`, `PUT /api/beacons/{id}/bookmark`, `POST/DELETE /api/beacons/{id}/check-in`, `DELETE /api/beacons/{id}` | `BeaconDetailView` (routes `.event`, `.beacon`, Map sheet) | Visual header + status pill, categories, title/schedule/location once, host only when shown; kind actions: RSVP / Request to join / pending (withdraw) / waitlisted (leave) / declined from the server response, bookmark optimistic with rollback (server value wins), check-in with contextual location + server geofence reasons (location needed / move closer / opens when the event starts), check-out, event chat via the resolver, people directory, open music, share URL, Apple Maps directions (destination only), show on map, creator delete; inline-markdown description | Yes (unit: check-in reasons, listing policy decode, markdown) | Pending | P0 PARITY | Replaced the generic `MapRouteDetailView`. Not implemented: share-to-chat card (§56.5), creator edit, guest-list import (§58), reminders scheduling (§59), geocoding "Current location" labels. |
| F44 | Event people directory | `GET /api/beacons/{id}/attendees/directory` | `EventDirectoryView` | A–Z / Interests / Mutuals; "Mutuals here" only when the server unlocks it and never duplicated under Everyone; relationship-aware rows (Your Click / via mutuals / shared interests); sort change scrolls to top; row → canonical profile (no connection ID, view-only) | No | Pending | P0 PARITY | Privacy of fields is server-decided. |
| F45 | Global search | Local inbox/groups/cached discovery/cached intents; `GET /api/chat/search?q` | `GlobalSearchView`, `SearchIndex`, `MessageHit` | Scopes All/Active/Archived/Groups/Nearby/Beacons/Intents; immediate local matches (name, shared interest, meet place, group name/member, beacon title/place/category, hub name/category, own availability posts); debounced (300 ms) cancellable server message search (≥2 chars); searching / results / no results / **error kept distinct** (server failure is never "No results"); explicit note that encrypted messages can't be searched server-side; routing: person → chat (profile via context menu), group → group chat, beacon → detail, hub → hub chat, message hit → its conversation, own intent → Home | Yes (unit: local matching + scopes, hit decode) | Pending | P0 PARITY | Message anchor (scroll to the hit) is not implemented; opens the conversation. Connection availability-intent matches need data the inbox bundle does not carry. |
| F46 | Regression / release hygiene audit | N/A | `DebugLaunch`, `Phase3Repository`, `HomeFeedModel` | `-preview-*` launch arguments (mock sessions, fixture chats) honored only in DEBUG; removed dead Phase 3 profile/timeline cache that nothing refreshed (Home greeting now seeds from the Me self-profile cache); single shared `FlowLayout`; generic `MapRouteDetailView` removed; Debug, Release, NotificationService, and ClickClip targets build | Yes (full suite: 125 tests / 28 suites) | Pending | P0 RELEASE | Performance (scroll/hitch, cold start), gesture feel, Dynamic Type, and VoiceOver passes require device verification — none performed. |
| F47 | Config regression: Supabase key missing | Info.plist | `Click/Info.plist`, `AppConfigTests` | Custom `INFOPLIST_KEY_*` settings are dropped by Xcode, which left `SUPABASE_ANON_KEY`, Google Sign-In IDs, `click://` URL scheme, and background modes out of the app. Every direct Supabase read (groups, notification prefs, location privacy, legacy group keys) failed and realtime never connected. Keys now live in `Click/Info.plist`; a test asserts they ship | Yes | Pending | P0 RELEASE | Root cause of "Groups nonexistent" and slow/non-live chats. Google Sign-In and deep links should be re-verified on device. |
| F48 | Prototype revision pass (9-23) | — | Root menus, Clicks "+", `NewGroupSheet`, Groups tab (groups + joined hubs/event chats), chat header (leading identity + menu), hugging bubbles with inline time and ✓/✓✓, beacon cards, timeline cache, profile registry + cached encounters/journal, profile Media grid / Files / human timeline, Map without title bar, rounded Nearby card, event detail sheet (hero, pills, host, RSVP state button, Event chat / Check in / Save / Directions, time·place·people card, About), Add Click (Group, Join hub, Create/Join Community Hub, How it works) | Ghost Mode removed (server flag cleared once per launch so no one stays hidden) | Partial (labels, beacon cards, cache, routing) | Pending | P0 PARITY | Joined hubs are remembered per device (no server listing exists). Beacon/event **creation** still not implemented. |
| F49 | Round-3 fixes (9-24) | `POST /api/beacons`, `/api/beacons/{id}/attendees/directory`, `hub_messages` (RLS), `/api/profile/timeline` (`target_type: chat`) | Chat: hugging bubbles w/ clipped event cards, 1:1 audio scrubber, older-history paging (`cursor`), reply strip fixed, composer blends (no container), "+" menu (Take Photo, Photo Library, Click Drop, Voice, Share Event/Beacon, File), Click Drops pixelated until 24 h reveal, beacon share cards. Clicks: group avatars → group info, uniform row height, hubs discovered from readable hub messages. Profile: Beacons tab second, Message/Nudge/Core/Click Drop tiles, all interests (shared highlighted), detailed encounter moments (KMP: weather+wind, dB, floor, light, motion, neighbourhood). Group profile: shared media/files/events open, common interests, group journal. Events: hero to top with Save/Share(Copy link, Share to chat, View on Map)/Close, one RSVP + three actions, People here preview, directory Best match / A–Z / Interests / Mutuals with rich rows; location no longer reopens the detail. Map: map-only menu, Nearby expanded covers the tab bar, "+" creates events/beacons. Home: reconnect card. QR: prefetched + rendered once | Partial (reconnect pick, best-match rank, beacon share metadata, Click Drop lock) | Pending | P0 PARITY | Sign-out loop and `-34018` were caused by unsigned (`CODE_SIGNING_ALLOWED=NO`) test installs; builds are now ad-hoc signed with entitlements. |
| F50 | Connectivity-aware offline notice (round 4 §1) | `GET /api/ping` (probe, available) | `NetworkMonitor`, `OfflineNotice(showing:hasCachedValue:refreshFailed:)` | "Offline — showing saved …" only when `NWPathMonitor` reports no path; online refresh failures show a quiet "Couldn't refresh · Retry"; Home discovery's missing location fix is `unavailable`, not stale | Yes (unit: `NetworkingTests` notice matrix) | Pending | P0 QUALITY | Sites: Home, Clicks, Me, Profile, Saved events. Airplane-mode and flaky-network behaviour still needs device verification. |
| F51 | Cancellation is not failure | N/A | `ModuleState.fail(_ error:)`, `Error.isCancellation`, `ClickAPIClient.send` | `CancellationError`/`URLError.cancelled`/`APIError.cancelled` restore the previous phase; chat and inbox models ignore cancellations; `CancellationError` no longer maps to `.server(-1)` | Yes (unit) | Pending | P0 QUALITY | Action-sheet error notices (≈50 sites) still use `userFacingMessage`; they run in user-initiated tasks that are not cancelled by navigation. |
| F52 | API client transport hardening | All | `ClickAPIClient.makeSession()`, `APIRequest.isIdempotent` | Dedicated ephemeral session: `waitsForConnectivity`, 20 s request / 60 s resource timeout, no URL cache; one jittered (300–800 ms) retry for GET/idempotent requests on timeout, dropped connection or 5xx; POST/PATCH never auto-retried; upload-task byte progress | Yes (unit: retry once, POST not retried, gives up after one retry) | Pending | P0 QUALITY | `POST /api/users/display-names` is marked idempotent (pure lookup). |
| F53 | Single-flight token refresh everywhere | `POST /auth/v1/token?grant_type=refresh_token` | `SessionController.validAccessToken()`, `refreshSession()` | Proactive refresh when <5 min remain; cold-start expired path now goes through the same single-flight task (previously called `authService.refreshToken` directly and could spend the refresh token twice) | Yes (unit: concurrent 401s → 1 refresh; cold start + concurrent call → 1 refresh; proactive window) | Pending | P0 COMPATIBILITY | `KeychainSessionVault(account:)` is injectable so tests never touch the real session item. Staging expiry roundtrip pending. |
| F54 | Freshness + identity caches | `POST /api/users/display-names` | `FreshnessCache`, `CachePolicy`, `IdentityCache` | Stale-while-revalidate with in-flight coalescing; one shared 1 h display-name cache replaces the separate lookups in `GroupRepository`, `ChatRepository.resolveNames`, `HubRepository.latest` (was 1 call per hub) | Yes (unit: fresh not refetched, stale refetched, concurrent misses coalesce) | Pending | P1 QUALITY | Windows: profile 5 min, beacon 2 min, engagement 60 s, hub info 10 min, names 1 h, inbox 60 s. Beacon cache is session memory only (not on disk). Hub latest-message fan-out limiting (top 10 while Groups visible) not yet done. Sign-out clears identity, beacon, pending-send, freshness and timeline caches. |
| F55 | Cold start without onboarding flash (§2) | `GET /api/users/{id}/profile` | `OnboardingCoordinator.adoptCachedCompletion`, `isResolved`, `AuthenticatedGateView`, `SessionController.evaluateAndAdmitSession` | Cached completed onboarding → shell immediately, server re-check in background (a failed re-check never bounces to onboarding); unresolved + no cache → `LaunchLoadingView` instead of onboarding chrome; fresh-token returning users are admitted without blocking on the profile gate; Home defers network until after the cached frame | Partial (existing onboarding tests) | Pending | P0 PARITY | Cold-start timing on device still pending. |
| F56 | Chat scroll and pagination (§3.1) | `GET /api/chat/messages?cursor` | `ChatView` | Auto-scroll only when the *last* message ID changes and the reader is near the bottom or sent it; `.scrollPosition(id:)` + explicit anchor restore after `loadOlder`; 300 ms debounced top sentinel guarded by `hasMoreHistory && !isLoadingOlder`; "Jump to latest" pill with unseen count; insert animation only after first paint | Partial (model tests) | Pending | P0 QUALITY | Gesture/scroll feel requires device verification. |
| F57 | Composer glass controls (§3.2) | N/A | `ComposerCircleLabel`, `glassCircleBackground` | "+", mic and send are the same 40 pt circle (`glassEffect` on iOS 26 via `#if compiler(>=6.2)` + `#available`, `.regularMaterial` otherwise); the trailing control is one view whose symbol morphs (`.symbolEffect(.replace)`) | Build only | Pending | P1 DESIGN | |
| F58 | Swipe-to-reply physics (§3.3) | N/A | `MessageBubbleView.replyGesture`, `SwipeReplyPhysics` | Rubber band `90·(1−e^(−dx/90))`; reply arrow fades/scales in from 20 pt and fills at 58 pt; one `.light` impact per crossing; vertical wins past 8 pt; spring(0.35, 0.7) back; incoming swipes right, outgoing left, in DMs/groups/hubs | Yes (unit: physics) | Pending | P0 QUALITY | Device gesture verification pending. |
| F59 | Stable send identity + send pipeline (§3.4) | `POST /api/chat/messages`, `/api/chat/media`, `/api/chat/attachments` | `ChatMessageItem.clientMessageID/stableID`, `PendingSendStore`, `ConversationModel.performSend/transmitPending` | Optimistic row keeps its client-ID identity through server replace, realtime echo and refresh (no re-insert, no duplicate); one path for text/media/beacon; retries reuse the client ID; failed rows show retry/✕ in place; pending and failed rows survive closing the chat and are restored on reopen; beacon share is now optimistic | Yes (unit: `ChatSendPipelineTests`) | Pending | P0 QUALITY | Hub sends use the same model path (text only). |
| F60 | Attachments: tray, progress, validation, voice (§3.4) | `/api/chat/media` (JSON base64, 25 MB, image/audio MIME list), `/api/chat/attachments` (2 MB) | `MediaValidator`, `StagedAttachmentTray`, `UploadStateOverlay`, `VoiceNoteRecorder`, `VoiceHoldStrip` | Pre-flight size/MIME check before encryption with clear copy; staging tray (multi-select up to 10 photos, file, reviewed voice note; removable; caption sent after media); paste image from + menu; drop images onto the chat; encrypting → byte-accurate upload ring (URLSession upload task); hold-to-record, slide left to cancel, slide up/tap to lock, live waveform, review (play/discard) before send; `metadata.waveform` (40 bins, additive, KMP ignores it) drawn in the bubble | Yes (unit: validator, waveform helpers via build) | Pending | P0 PARITY | Upload is still JSON base64 (server contract); progress covers request bytes. |
| F61 | Instant event cards (§3.5) | `GET /api/beacons/{id}` | `BeaconRepository.cachedBeacon/remember/prefetch/evict`, `BeaconDetailView.load` | Detail paints from cache (discovery results, previous opens, chat-card prefetch on appear), refreshes only when older than 2 min; engagement/RSVP/people load in parallel when the kind is known; delete evicts from cache and hides from map/Nearby via `AppRouter.deletedBeaconIDs` | Build only | Pending | P1 QUALITY | Cache is session memory (not disk). |
| F62 | Inbox rows equal height (§4 item 11) | N/A | `inboxRowChrome()`, `InboxPreviewText`, `InboxRowMetrics` | One owner of list insets/background for DM, group and hub rows (group/hub rows previously used the system default insets, ~22 pt taller); previews always reserve two lines | Yes (hosting-controller test: DM = group = hub height) | Pending | P1 DESIGN | |
| F63 | Shared conversation actions (§4, §29.7) | `PATCH /api/chat/messages/unread`, `POST /api/connections/hide`, `POST /api/safety/report`, `POST /api/safety/block`, group rename/leave/delete RPCs, `/api/hub/{id}` leave/delete | `DirectConversationActions`, `GroupConversationActions`, `HubConversationActions`, `ConversationActionDialogs`, `ConversationListModel` actions | One implementation for inbox swipe/context menus, the chat header menu, Profile, Group info and Hub screens. 1:1: Archive/Unarchive (full swipe), Mark Unread (swipe + menu), Core, Report with reason, Remove (confirm), Block (confirm, leaves chat); groups: Mark Unread, Rename (creator), Leave, Delete (creator); hubs: Leave (swipe), Delete (owner, when known) | Build + existing model tests | Pending | P0 PARITY | Remove uses `POST /api/connections/hide`; server `DELETE /api/connections` is the same per-user hide, so only one action exists. `JoinedHub.creatorID` is recorded when a hub is opened. |
| F64 | Core strip | N/A | `ClicksView.rememberStrip` | Tap opens the chat; touch-and-hold offers View Profile | Build only | Pending | P1 PARITY | |
| F65 | Hub preview fan-out cap (§1 item 16) | `GET /api/hub/messages?limit=1` | `ConversationListModel.refreshHubs`, `hubPreviewsVisible` | Latest-message previews for the 10 most recently active hubs only, and only while the Groups filter is visible | Build only | Pending | P1 QUALITY | |
| F66 | Prior connections after onboarding | `POST /api/connections/prior/respond {connection_id, action}` | `ConnectionItem.awaitsPriorResponse`, inbox "Knows you?" pill, Accept/Decline in the row/chat menu | Pending `source=prior` connections initiated by the other person show Accept/Decline; 409 `not_pending` is treated as already answered and refreshed | Build only | Pending | P1 PARITY | KMP surfaces this on the profile timeline; iOS surfaces it in the inbox row and chat menu. |
| F67 | Blocked people | `GET /api/safety/block` (new), `DELETE /api/safety/block?blocked_id=` | `BlockedUsersView` (Privacy → Blocked people), `ProfileRepository.blockedUsers/unblock` | List (names via identity cache), Unblock, honest load failure | Web route contract tests (click-web) | Pending | P1 PARITY | click-web: `app/api/safety/block/route.ts` now uses the bearer-aware `getSupabaseFromRouteRequest` for GET/POST/DELETE, so RLS (`blocker_id = auth.uid()`) sees mobile JWTs. Staging verification pending. |
| F68 | Nearby sheet without flicker (§5 item 12) | N/A | `NearbySheet` (`liveHeight`, `settledDetent`), `MapFeatureModel.settledDetent` | One `liveHeight` follows the finger with animations disabled during the drag; snap to the detent nearest the fling projection (`predictedEndTranslation`) with an interpolating spring; content is always mounted and fades by height (no mount mid-drag); constant horizontal inset; tab bar and floating controls change only after the snap settles | Yes (unit: opacity curve, projected snap) | Pending | P0 QUALITY | Drag feel needs device verification. |
| F69 | Map pin clustering + person callout (§5) | N/A | `MapFeatureModel.clusters`, `ClickMapView` | Grid clustering above a ~9 km span (cell = 1/10 of the span), count bubble zooms to fit; tapping a person pin shows "Name · first met at {place}" and tapping the callout opens the profile; Nearby rows already show distance | Yes (unit: clustering) | Pending | P1 PARITY | Map friction telemetry is tracked under F74. |
| F70 | Post-connect reveal (§28) | bind/confirm/QR responses | `PostConnectView`, `PostConnectModel` | Shown only for a server-confirmed match (Tap and QR). Avatars join with spring(0.5, 0.75); Reduce Motion fades. Heavy haptic on detection, success on connect. 1:1 shows "You Clicked at {place}" when the place is known; groups show stacked avatars and "Group created". Actions: Say hi, View profile, Open Groups | Build only | Pending | P0 PARITY | Deviation from KMP (spec wins): the reveal comes first and tags live on the same screen; KMP shows the tag sheet before a brief reveal. |
| F71 | Reconnect copy (§25) | `/api/connections?connectionId=` (encounters) | `PostConnectModel.load` | "Reconnected · {ordinal} time with {name}" from the real encounter count; "Extended Hangout" when the server merged this tap into the previous row (tag `Extended Hangout`); inbox refreshes after connect | Yes (unit: ordinal) | Pending | P0 PARITY | The bind response has no count or debounce flag, so both come from a follow-up encounter read. They are omitted if that read fails. |
| F72 | Context tagging (§26) | Supabase `connection_encounters` (GET + PATCH, RLS) | `EncounterContextRepository.saveContext`, `ContextTagTaxonomy`, `EncounterSensorSampler` | Suggested tags (KMP hour/location rules) and all 14 KMP tags, multi-select, custom label (≤25), Save or skip; copy for new, QR and reconnect. Writes use KMP `mergePatchLatestEncounter` semantics: rows from the last 30 min (+2 min skew), deduplicated union, sensor columns only on the user's own rows. Opt-in barometric elevation via `CMAltimeter` | Yes (unit: window, union, sensor ownership, suggestions) | Pending | P0 PARITY | Noise is not captured: there is no iOS opt-in UI. Elevation *category* is not derived (KMP uses relative altitude; iOS writes only `exact_barometric_elevation_m`). Multi-Tap people selection happens before tagging (existing host-selection screen). |
| F73 | Event recommendation after connect (§27) | `GET /api/connections/{id}/event-recommendation?lat&lng` | `PostConnectView.recommendationCard` | "Go together?" card with RSVP / Dismiss / open event; nil or error is silent and never blocks the connection | Build only | Pending | P1 PARITY | KMP shows this card only in the profile sheet; spec §27 puts it after connecting. |
| F74 | Telemetry (§71) | `POST /api/telemetry/connection-flow`, `POST /api/telemetry/friction` | `TelemetryQueue`, `ConnectionFlowTelemetry`, `FrictionTelemetry`, `AnonymizedHexbin` | KMP event names and 10% success sampling; failures always sent; persisted queue flushed on foreground/background; friction sessions ≥30 s with a pan; grass nudge; KMP-identical hexbin | Yes (unit: sampling, no PII, hexbin parity, persistence, transient failure) | Pending | P1 QUALITY | Catalog: `Docs/TELEMETRY.md`. Server receipt not verified. |
| F75 | Cross-platform connect compatibility (§8.1) | `/api/connections/proximity`, `/api/qr` | `ProximityCodec.randomToken`, `QRRedeemMessages`, `ConnectionDebugLog` (`-connection-log`) | iOS tokens never have adjacent repeats or a leading 0 (the KMP decoder can't hear those); QR errors map to KMP copy (expired, already used, not found, proximity); non-Click and non-UUID QR payloads are rejected locally; DEBUG request/response log overlay | Yes (unit: 5 000 generated tokens safe, error mapping) | Pending | P0 COMPATIBILITY | Test plan: `Docs/IN_PERSON_TEST_MATRIX.md`. The KMP decoder bug write-up is in that doc, ready to file. |
| F76 | Profile timeline moment (§6 item 5) | `/api/connections?connectionId=` (encounters) | `EncounterLabels.lines/badges/chips`, `TimelineRow` | KMP `ProfileConnectionMoment` lines shown directly (no Details disclosure): "Tue, Sep 22, 2026 · 7:04 PM"; place "Location • Neighbourhood, display" (`formatEncounterPlaceLine`); "61°F (16°C) · Clear · 7 km/h NE"; "Moderate · 58 dB"; "Below ground · 12 m"; light/motion/compass/battery as muted badges; context tags as chips with KMP labels; `vibe_capture` quote | Yes (unit: KMP formats, human-readable labels) | Pending | P1 PARITY | Noise and elevation labels now match KMP (LOUD = "Loud", ELEVATED = "Elevated"), replacing the earlier iOS-only wording. |
| F77 | Edit encounter tags | Supabase `connection_encounters` PATCH by id | `EncounterTagEditor`, `EncounterContextRepository.setTags`, `ContextTagPicker` (shared with post-connect) | "Edit tags" on any timeline encounter; the server-owned `at_event` tag is preserved | Build only | Pending | P1 PARITY | RLS update rights for both participants need staging verification. |
| F78 | Own profile: bio + remove photo | `PATCH /api/users/{id}/profile {bio}` (new), `DELETE /api/user/avatar` (new) | `EditProfileView`, `MeRepository.updateProfile/removeAvatar` | Bio ≤160 characters (an empty bio clears it), shown on Me and on peer profiles; Remove photo (confirm) clears `users.image` and deletes the stored objects | Web route tests (click-web) | Pending | P1 PARITY | click-web migration `20260924120000_users_bio.sql` is applied. The public profile does not expose the bio. |
| F79 | View-only public profile | `GET /api/users/{id}/public-profile` | `AppRoute.publicProfile`, `PublicProfileView` | People in event directories you aren't connected with open a limited card (name, avatar, aura), no Message/Nudge, with "Connect in person" guidance | Build only | Pending | P1 PARITY | Shared-note visibility on the other person's device: Pending (device). |
| F80 | Beacon location search (§7 item 13) | `POST/PATCH /api/beacons` (`metadata.location_name`, `formatted_address`, `lat`/`lon`) | `BeaconPlacePicker`, `PlaceSearchModel` (`MKLocalSearchCompleter`) | Search suggestions biased to the user, "Use current location" (reverse-geocoded name), map preview with a centre pin moved by dragging the map (renamed if moved >150 m); legacy "Current location" labels are reverse-geocoded on the detail view | Build only | Pending | P1 PARITY | |
| F81 | Custom categories + music link validation | `POST /api/beacons` | `BeaconFormRules`, `CreateBeaconSheet` | Free-text categories (≤3 total, ≤24 characters, case-insensitive dedupe) next to presets; soundtrack links must be http(s) links to a known music service | Yes (unit) | Pending | P1 PARITY | Artwork is not fetched client-side. |
| F82 | Beacon photos (§7 item 14) | `POST /api/beacons/image` (JSON base64 ≤2 MB, jpeg/png/webp/gif → `{image}`) → `metadata.image_url` | `CreateBeaconSheet.photoSection`, `BeaconRepository.uploadImage` | Library or camera on every beacon type; 4:3 cover-crop preview; JPEG compressed off the main actor under 2 MB; creator can replace or remove it (`image_url: null`) from Edit | Build only | Pending | P1 PARITY | |
| F83 | Creator edit | `PATCH /api/beacons/{id}` | `CreateBeaconSheet(editing:)`, `BeaconRepository.update` | Title, description, place, photo, schedule, categories, visibility, capacity, approval, guest-list visibility, show-name; the updated row replaces the cached beacon. Delete evicts it from the cache, map and Nearby | Build only | Pending | P0 PARITY | |
| F84 | Event reminders (§59) | Local notifications | `EventReminderScheduler` | T-60 and T-15 for events the user is going to or saved; honors Alerts → Event reminders (off cancels all); never prompts for permission; cancelled when un-RSVP'd and unsaved, and on sign-out; taps route via the existing `event_reminder` payload | Yes (unit: trigger times) | Pending | P1 PARITY | Server `event_reminder` pushes may also fire; de-duplication needs a product decision. |
| F85 | Guest list (§58) | `GET/POST /api/beacons/{id}/guest-list`, `POST …/guest-list/match` | `GuestListView` (event creator) | Counts (guests, on Click, invites), paste import (CSV or one per line), re-match, entries with truncated emails | Build only | Pending | P1 PARITY | Delete isn't supported by the server. |
| F86 | Join request pending | `GET /api/beacons/{id}/rsvp` | `BeaconDetailView.watchPendingRequest` | "Request sent…" copy; while pending, re-checks every 30 s on screen and announces approval or denial | Build only | Pending | P1 QUALITY | Hub leave: `DELETE /api/hub/{id}/participants/me` = `POST /api/hub/leave` (same gate, same delete). |
| F87 | "Message deleted" tombstones (§33) | `DELETE /api/chat/messages?messageId=` (+ tombstone write), `GET /api/chat/messages?include_tombstones=1` | `ChatMessageItem.isDeleted/tombstoned()`, `MessageBubbleView.deletedPlaceholder` | Own delete leaves a "Message deleted" placeholder in place (restored on failure); a live realtime DELETE turns the bubble into a placeholder; history shows server tombstones | Yes (unit: delete keeps tombstone, failure restores) | Pending | P1 PARITY | Backward-compatible: `messages` rows are still hard-deleted (KMP relies on the realtime DELETE); click-web migration `20260924121000_message_tombstones.sql` (applied) adds a service-role side table, returned only when `include_tombstones=1`. Hub messages have no tombstones. |
| F88 | Delete confirmation (§33) | `DELETE /api/chat/messages?messageId=` | `MessageBubbleView.confirmingDelete` | Context-menu Delete asks "Delete for everyone?" before the tombstone delete runs | Build only | Pending | P1 PARITY | Hub delete uses the same dialog. |
| F89 | Forward (§33) | `POST /api/chat/messages`, `POST /api/chat/media` per target | `ChatTargetPicker` (shared with event "Share to chat"), `ConversationModel.forward/canForward` | Pick Clicks or groups; text re-encrypts per target; media re-encrypts from the decrypted local copy (ciphertext never crosses chats); fresh client ID per target | Yes (unit: per-target sends, fresh IDs, eligibility) | Pending | P1 PARITY | Click Drops, beacon cards, tombstones and unsent rows can't be forwarded. |
| F90 | Save to Photos / Share (§37) | — | `PhotoLibrarySaver` (add-only), `ActivityShareSheet` | Images: "Save to Photos" with add-only permission; files and voice notes: system share sheet with the decrypted file | Build only | Pending | P1 PARITY | Locked Click Drops don't offer save. |
| F91 | Who reacted (§34) | `raw.reactions` / hub `reactions` rows | `ReactionSummary.userIDs`, `ReactorsSheet` | Long-press a reaction chip (or VoiceOver action) opens a sheet with a picker per emoji and resolved names and avatars | Yes (unit: own reaction tracked and removed) | Pending | P1 PARITY | Cached timelines from older builds have no reactor IDs until refreshed. |
| F92 | Unread divider (§31) | `POST /api/chat/messages/read` | `ConversationModel.firstUnreadID`, `UnreadDivider` | First unread incoming message is captured before mark-read; "New messages" row stays until the chat closes; the chat opens at the divider | Yes (unit: captured once, before read) | Pending | P1 QUALITY | Not shown in hubs (no receipts). |
| F93 | In-chat search + jump (§34) | `GET /api/chat/messages?aroundMessageId=`, `GET /api/hub/messages?aroundMessageId=` | `ChatSearchBar`, `ConversationModel.searchMatches/reveal/mergeWindow/returnToLatest`, `ChatRepositoryProtocol.fetchMessages(around:)`, `MessageFocus` | "Search" in the chat menu (all kinds); steps newest→oldest over decrypted loaded messages, loading older pages past the oldest match; Global Search message hits open the chat at the message; a window that isn't contiguous detaches until "Jump to latest" | Yes (unit: matching, overlap merge vs detach, return to latest, missing target, focus matching) | Pending | P1 PARITY | Only loaded (decrypted) history is searchable in encrypted chats. |
| F94 | Typing names in groups/hubs | Realtime typing broadcast | `ConversationModel.typingChanged/typingLabel` | "Lena is typing…", "Lena and Sam are typing…", "Lena and 2 others are typing…"; names from the timeline, then `IdentityCache` | Yes (unit: labels) | Pending | P2 QUALITY | Direct chats keep "typing…". |
| F95 | Icebreakers + 48 h say-hi warning (§42–43) | `ConnectionItem.sayHiDeadline` (server) | `Icebreakers` (KMP `IcebreakerRepository` prompts), `SayHiPanel` | Direct chats with fewer than 5 messages show 3 prompts (context-matched first, stable per connection), 15 s refresh cooldown, tap sends through the normal pipeline; the warning shows the server deadline | Yes (unit: context match, stable seed, count) | Pending | P1 PARITY | Vibe Check (§41) is **off** in KMP (`ChatViewModel.vibeCheckEnabled = false`), so it isn't surfaced; audit only. |
| F96 | Click Drop camera (§44) | `POST /api/chat/media` (`disposable_roll`, `collaboration_ttl`, optional `encounter_id`) | `ClickDropCameraView`, `ClickDropCamera` (AVCaptureSession), `ClickDropFilter` | Full-screen camera: flash, flip, shutter flash (skipped with Reduce Motion), status chip, then filtered preview (≤1280 px) with the ten KMP looks and a frame counter, Retake, Send; replaces the system picker for Click Drops only | Yes (unit: look order, 1280 px cap, every look renders, Noir grayscale, bad data) | Pending | P1 PARITY | Filter chain matches `ClickDisposableRollFilter.m` exactly; visual parity with KMP needs a device side-by-side. |
| F97 | Click Drop develop | `metadata.collaboration_ttl`; server `disposable_reveal` push (cron) | `ChatImageView.developTick`, `ConversationModel.nextClickDropReveal`, `ChatView.showToast` | The pixelated preview (kept) re-renders when the reveal passes while open; an in-chat toast says "Your Click Drop developed" | Build only | Pending | P2 QUALITY | No local notification: the server cron already pushes `disposable_reveal`, so a local one would duplicate. |
| F98 | Collaboration session (§44.1–44.2) | `ProximityMatch.encounterID/collaborationEndsAt` | `PostConnectModel.clickDropSession`, `AppEnvironment.clickDropSession`, `PostConnectView` "Send a Click Drop" | While the server window is open, post-connect offers "Send a Click Drop" (sends straight to the new Click with `encounter_id`); Drops sent from that chat in the window also carry it; the camera closes when the window ends | Yes (unit: encounter only for its connection inside the window) | Pending | P1 PARITY | Needs a real two-device tap to verify end to end. |
| F99 | Hub media (§62) | `POST /api/hub/media` (multipart), `GET /api/hub/media?hub_id&path`, `POST /api/hub/messages` (`message_type: image`) | `ChatRepository.sendHubMedia/uploadHubMedia/loadHubMedia`, `MultipartForm`, `APIRequest.multipart`, `ChatComposerView.photosOnly` | Hubs send photos and Click Drops (v2 hub epoch encryption, geofence coordinates, upload progress) and show hub photos from others (v2 and legacy); voice notes, files and event shares stay off in hubs (KMP parity); the "isn't available on iOS yet" state is gone | Yes (unit: hub media vector + digest, object path, multipart bytes, hub photo parse, Click Drop metadata) | Pending | P1 PARITY | Cross-client check (iOS ↔ KMP hub photo both ways) and a staging upload are Pending. |
| F100 | Availability overlaps (§9) | Supabase RPC `get_availability_overlaps {p_peer_ids}` | `MeRepository.availabilityOverlaps`, `HomeFeedModel.overlappingPeerIDs/overlapTitle`, `HomeView.overlapRow`, `APIRequest.supabaseRPC` (shared by groups and inbox previews) | While the viewer has live plans, Home shows "Lena is also free" / "3 Clicks are also free"; re-asked when plans or Clicks change; failures keep the last answer | Yes (unit: row parsing, titles) | Pending | P2 PARITY | RPC is RLS-scoped to mutual connections. |
| F101 | Verified group pre-check (§9) | RPC `verified_clique_edges_exist {p_member_ids}` | `GroupRepository.cliqueExists`, `CliqueEligibility`, `GroupMemberPickerSheet.ineligibleReason` | Picking someone checks every remaining candidate against them (`[viewer, a, b]`, cached, ≤6 at a time); ineligible people show "Hasn't Clicked with Lena" and are disabled; Create is blocked on a known conflict | Yes (unit: reasons, unknown pairs stay selectable) | Pending | P1 PARITY | Server still validates on `create_verified_clique`. |
| F102 | Group membership realtime | Realtime `group_members` (`*`, RLS-scoped; already in `supabase_realtime`) | `RealtimeStream.groupMembers`, `ChatRealtimeManager.onRowChanged`, `ConversationListModel.membershipRealtime` | Joins, leaves and removals refresh Groups (coalesced 1 s) without a pull | Build only | Pending | P2 PARITY | Needs a two-account staging check. |
| F103 | Remove group photo | `DELETE /api/groups/{id}/avatar` (**new**, click-web `feat/ios-round5`) | `GroupRepository.removeAvatar`, `GroupProfileView.removePhoto` | "Remove Group Photo" for members when a photo is set; same 60 s cooldown as upload | Yes (web jest: clear + scoped object removal, 403, 429, 401) | Pending | P2 PARITY | Deploy click-web before shipping. |
| F104 | Hub info + owner category (§61) | `GET /api/hub/{id}` (+`radius_meters`, additive), `PATCH /api/hub/{id} {category}`, `GET /api/hub/messages?limit=1` (`participant_ids`, `occupant_count`) | `HubInfoView`, `HubRepository.members/update`, `ChatView(hubMenu:onOpenHubInfo:)` | One options menu in hubs (Search, Hub Info, Rename/Delete or Leave); tapping the hub title opens info: type, category (owner picker), reach radius, here now, members with owner badge | Yes (unit: category labels) | Pending | P2 PARITY | Fixes a double "…" menu that §2.1's Search would otherwise have caused in hubs. |
| F105 | Appearance (§9 Settings) | Local (`appearance_mode`, legacy `dark_mode_enabled` kept in step) | `SettingsStore.appearance`, `MeView.appearanceSection` | System / Light / Dark menu; users who never chose now follow the system; an explicit legacy toggle maps to Dark/Light | Yes (unit: migration, legacy write-through) | Pending | P2 QUALITY | Web dashboard link already existed. **Blocker:** account deletion stays web-only (joinclick.co) until a native deletion route exists. |
| F106 | Push tap routing tests | APNs payload `type` | `ClickNotificationCoordinator.tapRoute(for:)` (pure), `handleNotificationTap` | Every `type` maps to one route; `disposable_reveal` opens the chat; missing IDs never navigate | Yes (unit: per type) | Pending | P1 QUALITY | NSE private fallback is now "Open Click to view it." (matches the server's hub push copy). |
| F107 | Audits: waitlist, Clicktivities | — | `AppRouter.parseConnectionInvocation` (`venue_id` only) | KMP `WaitlistDialog` and `ClicktivitiesScreen` are unreferenced in KMP; the only waitlist path is the email API used at auth. iOS keeps only `venue_id` QR parsing and doesn't surface Clicktivities | n/a (audit) | n/a | P3 AUDIT | No code added. |
| F108 | Accessibility pass (round-4 screens) | — | `VoiceHoldStrip`, `StagedAttachmentTray`, `BlockedUsersView`, `PublicProfileView` (+ reviewed `PostConnectView`, `NearbySheet`, `CreateBeaconSheet`, `GuestListView`) | Voice strip is one VoiceOver element with a live value; tray tiles grow with Dynamic Type; blocked rows stack at accessibility sizes with "Unblock Name"; profile name is a header and the aura is hidden | Build only | Pending | P1 QUALITY | Reviewed screens already honored Reduce Motion and labels; device VoiceOver run Pending. |
| F109 | Home entrance stability (item 9) | — | `HomeView` (animation keyed on card presence), `HomeFeedModel.isOpportunityPending/opportunityPlaceholderHeight` | Swapping one opportunity for another no longer re-animates every section; while its sources load, the card's height is held so sections below don't jump | Build only | Pending | P2 QUALITY | Device check of first paint with cold caches Pending. |
| F110 | Ambient noise + barometer context | `POST /api/connections/proximity` (`exact_barometric_elevation_m`, `noise_level`, `exact_noise_level_db`, as KMP), Supabase `connection_encounters` PATCH | `EncounterSensorSampler` (2 s `AVAudioRecorder` metering, KMP tiers and dB approximation), `ProximityEvidence.sensor`, `PostConnectModel.recordSensorContext`, Privacy → Encounter context | "Ambient sound" moved from Alerts to Privacy next to "Barometric context"; the tap sends barometric elevation with the handshake (mic is busy with ultrasonic); post-connect samples noise and patches the encounter; nothing is sampled unless opted in | Yes (unit: tiers, dB clamp, handshake keys) | Pending | P2 PARITY | `elevation_category` is server-derived (terrain-corrected) as in KMP, which never derives it on the client; proximity encounters get it only if the server adds enrichment there (it does for `/api/connections` and `/api/qr`). |
| F111 | Open decisions / blockers | — | — | **Event reminders:** server `event_reminder` pushes can duplicate the local T-60/T-15 reminders; needs a product decision (suppress local when the server sends, or drop server reminders for iOS). **Screenshots:** `-preview-*` capture needs a signed build. **KMP decoder bug:** write-up in `Docs/IN_PERSON_TEST_MATRIX.md`, not yet filed | n/a | Pending | P2 DECISION | Filing the KMP issue is outward-facing; waiting on the owner. |
| F112 | Server mobile auth verification (JWKS speedup) | `/api/*` (Supabase ES256 JWT validation against JWKS) | `lib/server/verifyBearerJwt.ts`, `lib/server/supabaseRouteAuth.ts` | Local verification of mobile ES256 bearer tokens against Supabase JWKS with fallback to `supabase.auth.getUser()`. Cuts mobile `/api/*` latency from 1,000–1,900 ms to <600 ms | Yes (unit in click-web) | Pending (deployment) | P0 PERFORMANCE | click-web branch `feat/ios-round5` commit `7c28c9f` ready for PR into main. |
| F113 | Chat timeline scrolling, pagination, and session reuse (item 4) | `GET /api/chat/messages?cursor` | `ChatView.swift`, `ConversationModel.swift`, `AppEnvironment.swift`, `ChatBackground.swift`, `HubChatView.swift` | Plain `LazyVStack(spacing: 2)`; non-observed `VisibleRows` via `onScrollTargetVisibilityChange`; older history triggered by `onScrollGeometryChange` near top only after user scroll; `ConversationModel` cached across navigations in `AppEnvironment` to prevent reloading and jump on re-entry; `ChatBackground` and `MessageRow` `Equatable` | Yes (unit: `conversationModelInstanceReuse`) | Pending | P0 QUALITY | Eliminates premature loading of older pages and timeline jumping to the top. |
| F114 | WhatsApp-style reactions overlay and system emoji picker (item 3) | `/api/chat/reactions`, `/api/hub/reactions` | `MessageActionOverlay.swift`, `EmojiKeyboardPicker.swift`, `MessageBubbleView.swift`, `ChatView.swift` | Full-screen blurred backdrop with lifted message bubble copy; 6-emoji capsule + "+" button above; actions panel below (Reply, Edit, Copy, Delete, Forward, Save); "+" triggers hidden `UITextField` system emoji keyboard with fallback Unicode emoji sheet | Yes (unit: `emojiKeyboardPickerFallbackList`) | Pending | P0 PARITY | WhatsApp parity layout with haptic feedback and safe area protection. |
| F115 | Tab bar slide with push/pop transition (item 7) | UIKit navigation controller | `TabBarPushPolicy.swift`, `ClickApp.swift`, `ChatView.swift` | Swizzled `UINavigationController.pushViewController` and `setViewControllers` setting `hidesBottomBarWhenPushed = true` before push; removed `.toolbar(.hidden, for: .tabBar)`; tab bar slides with push/pop and tracks interactive back swipe smoothly | Yes (unit suite passing) | Pending | P0 QUALITY | Replaced SwiftUI `.toolbar(.hidden, for: .tabBar)` with UIKit lifecycle swizzling. |
| F116 | Nearby real bottom sheet (item 5) | `GET /api/beacons`, `GET /api/hub/nearby` | `NearbySheet.swift`, `ClickMapView.swift`, `MapFeatureModel.swift` | Converted Nearby to native `.sheet` with `[.medium, .large]` presentation detents and 38pt corner radius; bottom `NearbyLip` floating card with 30pt radius over map; background interaction enabled at medium height | Yes (unit suite passing) | Pending | P0 PARITY | Eliminates custom drag/offset calculation and prevents sharp edge clipping. |
| F117 | Profile timeline encounter metric pills (item 10) | `/api/connections?connectionId=` (encounters) | `ProfileRepository.swift`, `ProfileView.swift`, `RevisionTests.swift` | Encounter row shows when line, place line, context tag pills with sparkle icon, and colorful metric pills in KMP order (condition, temperature, wind, noise, elevation, compass) using `FlowLayout(spacing: 6)` | Yes (unit: `metricPills`, `kmpFormats`, `chips`) | Pending | P1 PARITY | Noise category only (no dB); elevation shows category and/or meters; exact KMP hex colors and SF Symbols. |

## Phase 4 Direct Chat merge gate

Before the corrective Phase 4 PR is merged:
- [ ] GitHub CI build succeeds on the corrective branch.
- [ ] GitHub CI unit tests pass on the corrective branch.
- [ ] Legacy v1 direct history decrypts against a current production/staging fixture.
- [ ] Existing v2 direct history decrypts after an in-place KMP -> native update.
- [ ] Native v2 current write decrypts in the Android/current-client fixture harness.
- [ ] Staging realtime send/receive/read/delivery/typing roundtrip works between two accounts.
- [ ] Reaction/edit/delete persist after reload and propagate to the peer.
- [ ] Native edge-back, keyboard dismissal, swipe-reply, long-press menu, and timeline scroll ownership are device-tested.
- [ ] Standard APNs token registration and chat notification routing are device-tested.
- [ ] Chat frame pacing is measured on first open, warm open, keyboard presentation, rapid scroll, and pop-back.

A screenshot is evidence of appearance only; it is not a parity, transport, gesture, or performance verification.

Before PR #1 is considered verified rather than merely implemented:

- [ ] GitHub CI build succeeds.
- [ ] GitHub CI unit tests run and pass.
- [ ] In-place update over the current KMP iOS build preserves a valid session.
- [ ] Expired/invalid/offline session behaviors are exercised against staging or controlled mocks.
- [ ] Apple and Google provider sign-in are exercised on device.
- [ ] Profile Basics GET/PATCH is exercised against staging.
- [ ] Returning-user onboarding does not flash or restart completed steps.
- [ ] Camera/avatar upload works on device.
- [ ] Contacts discovery and prior-request flow work on device with test accounts.

The first two are repository merge checks. The remaining device/integration checks are release-readiness checks and may remain Pending when this implementation PR merges, but they must not be labeled Verified until performed.
