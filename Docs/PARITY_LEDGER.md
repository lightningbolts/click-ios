# Parity Ledger

Tracks implementation, testing, and parity status of every reachable Click flow.

"Automated" records only tests that actually exist in this repository. "Device-Tested" remains Pending until the flow has been exercised on physical hardware or an in-place TestFlight update as appropriate. A green CI build does not count as device verification.

| ID | Flow | Backend Route | Native Entry | States Implemented | Automated | Device-Tested | Classification | Notes |
|---|---|---|---|---|---|---|---|---|
| F01 | Legacy Session & State Migration | N/A (Keychain & UserDefaults) | `LegacyKMPStateMigrator`, `KeychainSessionVault` | Decode, missing/invalid record, JWT sub derivation, retired-key cleanup, non-destructive session replacement | Partial (unit fixtures) | Pending | P0 COMPATIBILITY | In-place KMP -> Swift Keychain compatibility still requires physical update-install verification; later queue/cache domains remain preserved for phase-specific migration. |
| F02 | Settings Preference Migration | N/A (UserDefaults) | `SettingsStore` | Legacy suite reads/writes, per-user onboarding cache, session reset | Yes (unit) | Pending | P0 COMPATIBILITY | Uses `click_auth_prefs`; retired call/home-layout behavior is not restored. |
| F03 | Root Gate & App Launch | Self profile + auth | `RootGateView`, `AppEnvironment` | Restoring, Auth, Profile Basics, onboarding resolution, retryable onboarding-load failure, Main | Partial | Pending | P0 PARITY | Full cold/warm/offline/update gate sequence needs integration/device coverage. |
| F04 | Typed Routing & Deep Links | N/A | `AppRouter` | Per-tab paths, pending intent, connection invocation, event/hub routes | Yes (unit) | Pending | P0 PARITY | `/c/*` routes to Add Click and preserves `token/qr_token/qt`, expiry/issued-at seconds or ms, and `venue_id`. |
| F05 | API Client Foundation | `/api/*` | `ClickAPIClient` | Bearer injection, 401 refresh, exactly-one retry, HTTP error taxonomy | Yes (unit) | Pending | P0 PARITY | Single-flight behavior is provided by `SessionController.refreshSession`; multi-request concurrency still merits an integration test. |
| F06 | App Clip Shell | `/c/{uuid}` | `ClickClip` | Target/shell only | Pending | Pending | P0 COMPATIBILITY | Full invocation/redeem path is later work. |
| F07 | Notification Service Extension | APNs payload | `NotificationService` | Category inspection, legacy v1 preview decrypt, privacy-safe fallback for v2/unknown payloads | Pending | Pending | P0 COMPATIBILITY | V2 preview decryption is intentionally not claimed until extension-safe epoch-key access is implemented and device-tested. |
| F08 | Supabase Auth Service | Supabase Auth `/auth/v1/*` | `SupabaseAuthService`, `AuthView` | Email/password, verification-required signup, Apple ID-token exchange, Google OAuth callback session, refresh | Partial | Pending | P0 PARITY | Password rules are tested; provider exchanges require staging/device verification. |
| F09 | Session Controller & Restore Policy | Supabase Auth + self profile | `SessionController` | Restore, freshness check, refresh, offline identity, hard-auth eviction, profile gate, sign out | Partial | Pending | P0 PARITY | Critical state transitions are implemented but still need dedicated mocked integration tests and physical update-install validation. |
| F10 | Profile Basics Gate & Sync | `GET/PATCH /api/users/{userId}/profile` | `ProfileBasicsGateView`, `SessionController` | Server-derived gate, 13+ validation, durable PATCH | Partial | Pending | P0 PARITY | Endpoint contract verified against click-web; device/staging round-trip not yet verified. |
| F11 | Design System Brand Alignment | N/A | `Typography.swift`, `Colors.swift`, `ClickLogo` | Manrope hierarchy, Click purple semantic palette, canonical mark | Partial (font load) | Pending | P1 DESIGN | Visual/dark-mode/Dynamic Type review still pending. |
| F12 | Permission Coordinator | iOS frameworks | `PermissionCoordinator` | Camera, Photos, Contacts, Microphone, Location, Calendar, Bluetooth state, Notifications, Settings handoff | Pending | Pending | P0 PARITY | Contextual API exists; each OS permission path still needs device verification. |
| F13 | Native Camera & Avatar Upload | `POST /api/user/avatar` | `AvatarService`, `NativeCameraPicker` | Capture/library, normalize/downsample, JPEG <=2MB, upload | Partial (image processing) | Pending | P0 PARITY | Real camera + authenticated upload must be tested on device. |
| F14 | Privacy Contact Discovery | `POST /api/contacts/discover`, `POST /api/connections/prior/request` | `ContactDiscoveryService`, `PriorConnectionsView` | Phone/email normalization, SHA-256, match display, known-since, request | Partial (normalization/hash) | Pending | P0 PARITY | Privacy disclosure now matches behavior; real Contacts permission and backend matching require device/staging validation. |
| F15 | Onboarding Flow & Server Reconciliation | `GET/PATCH /api/users/{userId}/profile` | `OnboardingRepository`, `OnboardingCoordinator` | Welcome, Interests, Personality, Avatar, Prior Connections, loading/error/retry, returning-user reconciliation | Partial (state-machine/unit) | Pending | P0 PARITY | Server saves are implemented; full returning-user/new-user flows still need staging/device verification. |
| F16 | End-to-End Encryption (V1 & V2) | `/api/chat/devices`, `/api/chat/epochs` | `ClickCryptoV1`, `ClickCryptoV2`, `DeviceIdentityVault`, `ChatRepository` | V1 legacy decrypt/write; V2 device discovery/registration, epoch discovery/create/rotate, recipient unwrap, envelope encrypt/decrypt, fail-closed writes | Yes (primitive unit coverage) | Pending | P0 PARITY | Crypto primitives are covered; Android/web cross-client fixtures, existing-device historical-key transfer, and physical update-install compatibility are still merge/release gates. |
| F17 | Direct Chat Messaging API | `/api/chat/messages`, `/api/chat/reactions` | `ChatRepository`, `ConversationModel` | Canonical chat resolution, fetch/send, encrypted edits, soft delete, persisted reactions, read/delivery, rollback-safe optimistic mutations | Yes (model unit coverage) | Pending | P0 PARITY | Plaintext fallback was removed. Live backend roundtrip and cross-client crypto compatibility remain unverified. |
| F18 | Realtime Message Streaming & Presence | Supabase Realtime WebSocket | `ChatRealtimeManager` | Phoenix join/heartbeat/reconnect, postgres insert/update/delete, typing broadcast/decay, decrypted realtime ingestion | Partial | Pending | P0 PARITY | Requires staging socket verification with token refresh/rebind and two real clients; no simulator-only claim is treated as transport verification. |
| F19 | Direct Chat UI & Interactions | N/A (SwiftUI Interface) | `ChatView`, `ConversationModel`, `MessageBubbleView`, `ChatComposerView` | Native push/pop and interactive back, hidden tab chrome in chat, date separators, receipts, direction-locked swipe reply, context menu/reactions, reply/edit composer, interactive keyboard dismissal, near-bottom scroll ownership | Partial | Pending | P1 QUALITY / P0 PARITY | Preview screenshots are not treated as quality verification. Physical-device gesture, keyboard, frame-pacing, VoiceOver, and rapid-scroll validation remain required. |
| F20 | Standard APNs Registration & Routing | `POST /api/user/push-tokens` + APNs payload | `ClickAppDelegate`, `ClickNotificationCoordinator` | Existing-permission re-registration, secure pre-auth token queue, standard-token upload, user-scope protection, typed chat/event/hub/connection routing | Partial | Pending | P0 PARITY | Permission is not prompted on launch. APNs token rotation, notification tap routing, and foreground behavior require device verification. |
| F21 | Home (linear feed) | `/api/me/recap`, `/api/me/nudges` (+`/{id}/dismiss`, `/{id}/acted`), `/api/user/availability-intents`, `/api/me/event-bookmarks`, `/api/beacons`, `/api/hub/nearby`, inbox via shell `ConversationListModel` | `HomeView`, `HomeFeedModel`, `AvailabilitySheet` | Prototype hierarchy (greeting/search → I'm down for… → one social opportunity → recent people strip + secondary nudge → recap Day/Week → saved & upcoming → nearby counts → insights); per-module cached/loading/fresh/confirmed-empty/failed/unavailable states; no fake-zero recap; discovery unavailable without existing location permission (never prompts); opportunity priority (live/today saved event → live nearby event → shared-event nudge → <12h say-hi → reconnect nudge → nearby today); nudge dismiss/acted with cache pruning | Yes (unit: opportunity priority, module states, recap decode, beacon/bookmark decode, card-visual golden parity) | Pending | P0 PARITY | Featured event now sourced from real bookmarks/nearby events; nearby counts from real `/api/beacons` + `/api/hub/nearby`. Server `/api/me/recap` returns zeros with 200 on some internal failures — that server behavior cannot be distinguished client-side. Visual/Dynamic Type/device scroll verification pending. |
| F22 | Saved Events | `GET /api/me/event-bookmarks` | `SavedEventsView` (route `.savedEvents`) | Cached seed, refresh, upcoming vs past/unavailable sections, stale notice, error w/ retry, confirmed empty, rows → canonical `.event` route | Partial (decode) | Pending | P0 PARITY | Deleted events render as unavailable (not tappable). |
| F23 | Deterministic beacon visuals | N/A (port of click-web `generateCardVisual`) | `CardVisual`, `EventVisual` | FNV-1a seed, weighted hue buckets, pattern, WCAG scrim; uploaded image override | Yes (golden fixtures from web) | Pending | P0 PARITY | Pattern ink drawn with a static `Canvas`; no scroll-time animation. |
| F24 | Me root (identity/account home) | `GET /api/users/{id}/profile`, `PATCH /api/user/availability`, `/api/user/availability-intents`, `/api/me/event-bookmarks`, `POST /api/user/avatar` | `MeView` (route `settings`, label Me) | Identity-first header (avatar edit, name, Clicks count, availability status), Core strip from real inbox data, grouped regions (Free currently · Availability post · Saved events · Calendar / Alerts · Privacy & data · Permissions · Interests · Personality / Dark mode · Web dashboard / Sign out · Delete account); Free currently pending→server echo→saved, failure alert + revert; unavailable (not "off") when profile read fails | Yes (unit: self-profile decode, free-currently echo requirement) | Pending | P0 PARITY | Me tab avatar now seeded from the same self-profile read. `SettingsStore` preferences made observable so Dark mode applies live. |
| F25 | Alerts (notification preferences) | Read `notification_preferences` under RLS (KMP contract), write `PATCH /api/user/preferences` | `AlertsSettingsView` | Message notifications, Event reminders, Reconnect nudges, Availability matches, Hub messages; per-toggle pending state; server row is truth; failed read is an error (never "all on"); OS permission denied → Settings handoff; notDetermined → request only after user intent; Ambient sound enrichment requests microphone contextually | Yes (unit: defaults vs failure, save echo) | Pending | P0 PARITY | Event teasers (Seed-a-Room) and call notifications intentionally absent. Server-side push gating is backend behavior (not verified from iOS). |
| F26 | Privacy & data | `PATCH /api/user/ghost-mode`; `users.location_*` columns via RLS PostgREST (KMP contract) | `PrivacySettingsView`, `GhostModeController` | Ghost Mode session-scoped (local immediately, server bit synced, reset every launch), Location snap / Memory Map / Business insights independent server-confirmed toggles (zero-row write = failure), Location snap requests location contextually, Barometric context local opt-in | Yes (unit: zero-row write failure, defaults off) | Pending | P0 PARITY | Ghost effects on Map/Nearby/typing are implemented in their slices (see F27+). |
| F27 | Permissions hub | iOS frameworks | `PermissionsSettingsView` | Location, Bluetooth, Microphone, Camera, Photos, Contacts, Calendar, Notifications; Allow only when not determined, otherwise Settings; refresh on foreground | No | Pending | P0 PARITY | Bluetooth has no standalone request API; it is requested by Tap to Connect. |
| F28 | Interests / Personality editing | `PATCH /api/users/{id}/profile` (`tags`, `personality_tags`) | `InterestsSettingsView`, `PersonalitySettingsView` | Starts from server truth (never an empty selection), ≥5 interests, exactly 5 traits ("Pick exactly 5 traits."), save error keeps editor open | Partial (repository shared with onboarding) | Pending | P0 PARITY | Reuses the onboarding pickers with Settings copy. |
| F29 | Calendar | EventKit (read-only) | `CalendarSettingsView` | Status, contextual request, Settings handoff, privacy copy | No | Pending | P0 PARITY | No mutual-free-time consumer exists natively yet; copy does not claim one. |
| F30 | Account deletion | Web `DELETE /api/user/delete` (cookie auth) | `MeView` → confirmation → `joinclick.co/?tab=settings` | Explains permanence; opens the web settings page that hosts the name-confirmed deletion flow; never sends the bearer token | No | Pending | P0 RELEASE | **Blocker:** no bearer-authenticated deletion endpoint exists; completion happens on web and requires a web sign-in. Sign in with Apple token revocation is not verifiable from this repo. |
| F31 | Add Click root | N/A | `AddClickView` | Tap to Connect hero with one Start action, My QR / Scan quick actions, "How it works"; no unfinished rows (retired "Join hub by venue code" has no backend contract and is not shown) | No | Pending | P0 PARITY | Hub creation is surfaced from Map (hub creation lives with map/beacon creation). |
| F32 | My QR | `GET /api/qr` | `MyClickCodeView` | Identity header, 90 s token, refresh ~10 s before expiry, code hidden the moment it expires, fast retry on failure, refresh on foreground, share URL, screen kept awake while visible | No | Pending | P0 PARITY | Single `TimelineView` for the countdown (no per-render timers). |
| F33 | Tap to Connect (Tri-Factor) | `POST /api/connections/proximity`, `GET ?pending_handshake_id`, `POST /api/connections/proximity/confirm` | `TapConnectView`, `TapConnectModel`, `BLEProximityService`, `UltrasonicService`, `ProximityCodec`, `ProximityRepository` | idle → preparing (contextual mic/Bluetooth/location) → needsPermission (mic denied / Bluetooth unauthorized / off) → sensing (BLE advertise+GATT token read, 18.5 kHz chirp+digits, progressive GPS ≤6.5 s) → submitting → connected / encounter saved (already connected) / waitingForPeer (12×2.5 s recovery, resumes on foreground) / choosingPeople (≤11 peers) → confirming / savedOffline (user-scoped queue, flushed on next launch) / failed + retry + QR fallback; cancel/background stop radios and mic immediately | Yes (unit: token normalize, synthesis timing, ultrasonic round-trip incl. repeated digits + multi-peer + noise rejection, bind-result parsing, request body) | Pending | P0 PARITY | Wire-compatible with KMP (service/characteristic UUIDs, token GATT payload, chirp+digit synthesis, request body). Native uses a chirp-synchronized Goertzel decoder because the KMP decoder's filter state is incorrect and merges repeated digits (emitted audio unchanged). Location only when Location snap is on and Ghost Mode is off. **Not yet implemented:** post-tap context tagging (§26), event recommendation (§27), encounter tether (§45), connection-flow telemetry (§71.2). Requires two/three-device hardware verification (BLE, ultrasonic, GPS, Multi-Tap) — none performed. |
| F34 | Map + Nearby | `GET /api/beacons`, `GET /api/hub/nearby`, `GET /api/beacons/{id}` (focus), dashboard bundle `map` rows (pins) | `ClickMapView`, `MapFeatureModel`, `NearbySheet` | One model for map + list: stable annotation identity (people/beacons/hubs), layers (mirrors web `MapLayerToggles` + hubs), chip filter applied to both map and list, local search, selection (beacon → detail sheet, hub → hub route, person → canonical profile), focus intents from Home/Saved/deep links (`MapFocus`), location states (not determined → "Turn on", denied → Settings, approximate/precise), live location only while visible, viewport-coalesced refetch (≥1 km), cached discovery seed, loading / error (never shown as empty) / confirmed empty; Nearby lip → medium → expanded with header-only drag (list keeps native scrolling) | Yes (unit: first-meet pin + peer collapse, geo_location precedence, layer mapping) | Pending | P0 PARITY | Custom lip container used because a system sheet would cover the native tab bar on the Map root. Not implemented: beacon/event/hub **creation** (no "+" is shown), map-friction telemetry + grass nudge (§71.1), intent-match signals in Nearby. |
| F35 | Ghost Mode effects on Map | `PATCH /api/user/ghost-mode` | `MapFeatureModel.ghostModeChanged`, toolbar toggle | Stops live location, hides own dot, pauses discovery requests (no coordinates sent), grayscale/muted map, banner copy | No | Pending | P0 PARITY | Typing-presence suppression is applied in chat (see chat rows). |
| F36 | Peer profile (canonical) | `GET /api/users/{id}/profile`, `GET /api/connections?connectionId=` (event-redacted encounters), `/api/profile/timeline` (GET/POST/PUT/DELETE), `GET /api/connections/{id}/tabs`, `POST /api/safety/report`, `POST /api/safety/block`, `DELETE /api/connections?connectionId=` | `ProfileView`, `PeerProfileModel`, `JournalEditor` | Centered identity, Message primary / Nudge secondary (nudge is a normal encrypted message), relationship line from real encounters only, common ground + personality, chip tabs Timeline / Media / Links / Files / Beacons each with independent loading/error/empty, Links extracted on-device from decrypted messages (http/https only), journal add/edit/delete via sheet (private/shared), safety menu (remove / report / block) with confirmation — UI changes only after server success | Partial (link extraction) | Pending | P0 PARITY | Encounter event fields render only when the server's per-viewer eligibility returned them. Media/Files tabs list items with dates; opening a profile media item in the viewer is not wired yet. |
| F37 | Verified group profile + management | RLS reads `group_members`/`chats`/`groups`/`messages`; RPCs `rename_clique`, `leave_clique`, `delete_clique`, `create_verified_clique`; `POST/DELETE /api/cliques/members`; `POST /api/groups/{id}/avatar`; `/api/chat/devices` + `/api/chat/epochs` | `GroupProfileView`, `GroupRepository`, `GroupMemberPickerSheet`, `ChatRepository.reconcileMembershipEpoch` | Identity (custom or generated two-member avatar), Message primary, members with Creator role, creator-only add (eligible active Clicks) / remove (confirm) / rename / photo / delete, leave for everyone; every add/remove is followed by E2EE v2 epoch reconciliation and the UI reports: rotated · still legacy (not every member has a v2 device) · **membership saved but securing failed** with a "Finish Securing Group" retry; manual creation seals the 32-byte master per member with the KMP pairwise rule, then initializes v2 when possible | Yes (unit: creation payload unwrap per KMP rule, missing-edge rejection) | Pending | P0 PARITY | Legacy (non-v2) groups cannot rotate their master on removal (protocol limitation) — surfaced honestly. Staging verification of add/remove + rotation with KMP/web members not performed. |
| F38 | Conversation kinds (direct / verified group / hub) | `/api/chat/*` (direct+group), `/api/hub/messages`, `/api/hub/messages/{id}`, `/api/hub/reactions`, `/api/hub/devices`, `/api/hub/epochs`, `/api/hub/{id}`, `/api/hub/join`, `/api/hub/{id}/participants/me`, Edge Function `verify-hub-proximity`, `GET /api/beacons/{id}/event-chat` | `ConversationKind`, `ChatRepository`, `ChatRealtimeManager` (`.chat`/`.hub`/`.inbox`), `ConversationModel`, `ChatView`, `HubChatView`, `EventChatView` | One conversation feature parameterized by kind. Groups: legacy `e2e_grp:` read/write via master unwrapped from `group_members.encrypted_group_key` (KMP rule), v2 via chat epochs with server-re-read membership; sender names per run. Hubs: v2 when upgraded (hub epochs, KMP rotation rule), plaintext only for never-upgraded hubs (KMP behavior), legacy hub keys read-only, geofence evidence only when a fix exists, real access reasons (not near / location needed / RSVP / ended), no receipts, owner rename/delete, leave (standalone). Event chat always via the resolver (200/403/404/409/410 bounded states). Ghost Mode suppresses outgoing typing. | Yes (unit: hub receipts, identity helpers, hub error mapping, resolver mapping, legacy hub key golden) | Pending | P0 PARITY | Hub timeline has no older-page cursor server-side (latest 120 only). Hub **media** is not supported on iOS (shown as unavailable). Cross-client group/hub v2 verification on staging not performed. |
| F39 | Inbox realtime | Realtime `messages` INSERT (RLS-scoped) | `ConversationListModel.startRealtime/applyInserted` | Independent inbox subscription; known direct/group rows update in place (last message, time, unread, move to top), open conversation not counted unread, unknown chat → one coalesced refresh; rebinds with a fresh token on foreground, torn down in background | Yes (unit: in-place update + unread) | Pending | P0 PARITY | Token rotation while foregrounded relies on reconnect; §35 re-auth push over an open socket is not implemented. |
| F40 | Chat media (image / voice note / file) | `POST /api/chat/media`, `POST /api/chat/attachments`, `POST /api/chat/attachments/sign`, `POST /api/chat/messages` | `ChatMedia` (`MessageMedia`, `AttachmentEnvelope`, `ChatMediaVault`), `ClickCryptoV2` media, `ClickCryptoV1` media bytes, `ChatMediaViews` (`AudioPlaybackService`, `VoiceNoteRecorder`, image/audio/file bubbles, `MediaViewer`, attachment menu) | v2 chats: `encryptMedia` + `type=media` authorization bound to the message's client ID (server gate), `media_*` metadata; legacy chats: KMP `encryptMediaBytes` with pairwise / group-master keys, files via `ccx:v1:` per-file key + plaintext SHA-256; downloads re-sign expired URLs, verify, decrypt once into a protected cache vault (cleared on sign-out); reserved image box, one fullscreen viewer (zoom/pan/share/drag-dismiss), Quick Look for files, one shared audio player (seek slider wins over swipe-to-reply, interruption/route handling), contextual mic permission, one recording session, optimistic bubbles with scoped retry | Yes (unit: **click-web v2 media vector decrypt + authorization AAD**, tamper/wrong-chat rejection, legacy bytes, base64 payload normalization, descriptors, parsing) | Pending | P0 PARITY | Plaintext never uploaded. Not implemented: Click Drop/disposable capture flow, video, profile-tab media viewer. Device verification of recording/playback/QuickLook pending. |
| F41 | Reply metadata privacy | `POST/PATCH /api/chat/messages` | `ChatRepository`, `ConversationModel.resolveReplyQuotes` | Native sends only `reply_to_id` for encrypted chats and resolves quotes from the decrypted local timeline; reads `reply_to_content` (web/KMP) and legacy `reply_to_snippet` | No | Pending | P0 RELEASE | **Cross-client issue:** web (`useChatEncryption`) and KMP write a plaintext `reply_to_content` excerpt into message metadata for E2EE chats, exposing message text to the server. Native no longer does; other clients still do. |
| F42 | Conversation background | N/A | `ChatBackground` | Stationary, deterministic low-contrast wash + static motif seeded per conversation over the neutral chat surface; no network, present on first frame, never animates on scroll | No | Pending | P1 | Seeded by connection/chat ID. Encounter-context seeding, presets, and custom images are not implemented (no backend field exists for a chosen background). |
| F43 | Canonical beacon / event detail | `GET /api/beacons/{id}`, `GET/POST/DELETE /api/beacons/{id}/rsvp`, `GET /api/beacons/{id}/engagement`, `PUT /api/beacons/{id}/bookmark`, `POST/DELETE /api/beacons/{id}/check-in`, `DELETE /api/beacons/{id}` | `BeaconDetailView` (routes `.event`, `.beacon`, Map sheet) | Visual header + status pill, categories, title/schedule/location once, host only when shown; kind actions: RSVP / Request to join / pending (withdraw) / waitlisted (leave) / declined from the server response, bookmark optimistic with rollback (server value wins), check-in with contextual location + server geofence reasons (location needed / move closer / opens when the event starts), check-out, event chat via the resolver, people directory, open music, share URL, Apple Maps directions (destination only), show on map, creator delete; inline-markdown description | Yes (unit: check-in reasons, listing policy decode, markdown) | Pending | P0 PARITY | Replaced the generic `MapRouteDetailView`. Not implemented: share-to-chat card (§56.5), creator edit, guest-list import (§58), reminders scheduling (§59), geocoding "Current location" labels. |
| F44 | Event people directory | `GET /api/beacons/{id}/attendees/directory` | `EventDirectoryView` | A–Z / Interests / Mutuals; "Mutuals here" only when the server unlocks it and never duplicated under Everyone; relationship-aware rows (Your Click / via mutuals / shared interests); sort change scrolls to top; row → canonical profile (no connection ID, view-only) | No | Pending | P0 PARITY | Privacy of fields is server-decided. |
| F45 | Global search | Local inbox/groups/cached discovery/cached intents; `GET /api/chat/search?q` | `GlobalSearchView`, `SearchIndex`, `MessageHit` | Scopes All/Active/Archived/Groups/Nearby/Beacons/Intents; immediate local matches (name, shared interest, meet place, group name/member, beacon title/place/category, hub name/category, own availability posts); debounced (300 ms) cancellable server message search (≥2 chars); searching / results / no results / **error kept distinct** (server failure is never "No results"); explicit note that encrypted messages can't be searched server-side; routing: person → chat (profile via context menu), group → group chat, beacon → detail, hub → hub chat, message hit → its conversation, own intent → Home | Yes (unit: local matching + scopes, hit decode) | Pending | P0 PARITY | Message anchor (scroll to the hit) is not implemented; opens the conversation. Connection availability-intent matches need data the inbox bundle does not carry. |
| F46 | Regression / release hygiene audit | N/A | `DebugLaunch`, `Phase3Repository`, `HomeFeedModel` | `-preview-*` launch arguments (mock sessions, fixture chats) honored only in DEBUG; removed dead Phase 3 profile/timeline cache that nothing refreshed (Home greeting now seeds from the Me self-profile cache); single shared `FlowLayout`; generic `MapRouteDetailView` removed; Debug, Release, NotificationService, and ClickClip targets build | Yes (full suite: 125 tests / 28 suites) | Pending | P0 RELEASE | Performance (scroll/hitch, cold start), gesture feel, Dynamic Type, and VoiceOver passes require device verification — none performed. |
| F47 | Config regression: Supabase key missing | Info.plist | `Click/Info.plist`, `AppConfigTests` | Custom `INFOPLIST_KEY_*` settings are dropped by Xcode, which left `SUPABASE_ANON_KEY`, Google Sign-In IDs, `click://` URL scheme, and background modes out of the app. Every direct Supabase read (groups, notification prefs, location privacy, legacy group keys) failed and realtime never connected. Keys now live in `Click/Info.plist`; a test asserts they ship | Yes | Pending | P0 RELEASE | Root cause of "Groups nonexistent" and slow/non-live chats. Google Sign-In and deep links should be re-verified on device. |
| F48 | Prototype revision pass (9-23) | — | Root menus, Clicks "+", `NewGroupSheet`, Groups tab (groups + joined hubs/event chats), chat header (leading identity + menu), hugging bubbles with inline time and ✓/✓✓, beacon cards, timeline cache, profile registry + cached encounters/journal, profile Media grid / Files / human timeline, Map without title bar, rounded Nearby card, event detail sheet (hero, pills, host, RSVP state button, Event chat / Check in / Save / Directions, time·place·people card, About), Add Click (Group, Join hub, Create/Join Community Hub, How it works) | Ghost Mode removed (server flag cleared once per launch so no one stays hidden) | Partial (labels, beacon cards, cache, routing) | Pending | P0 PARITY | Joined hubs are remembered per device (no server listing exists). Beacon/event **creation** still not implemented. |

## Phase 4 Direct Chat merge gate

Before the corrective Phase 4 PR is merged:
- [ ] GitHub CI build succeeds on the corrective branch.
- [ ] GitHub CI unit tests pass on the corrective branch.
- [ ] Legacy v1 direct history decrypts against a current production/staging fixture.
- [ ] Existing v2 direct history decrypts after an in-place KMP -> native update.
- [ ] Native v2 current write decrypts in the Android/current-client fixture harness.
- [ ] Staging realtime send/receive/read/delivery/typing roundtrip works between two accounts.
- [ ] Reaction/edit/delete persist after reload and propagate to the peer.
- [ ] Native edge-back, keyboard dismissal, swipe-reply, long-press menu, and timeline scroll ownership are device-tested.
- [ ] Standard APNs token registration and chat notification routing are device-tested.
- [ ] Chat frame pacing is measured on first open, warm open, keyboard presentation, rapid scroll, and pop-back.

A screenshot is evidence of appearance only; it is not a parity, transport, gesture, or performance verification.

Before PR #1 is considered verified rather than merely implemented:

- [ ] GitHub CI build succeeds.
- [ ] GitHub CI unit tests run and pass.
- [ ] In-place update over the current KMP iOS build preserves a valid session.
- [ ] Expired/invalid/offline session behaviors are exercised against staging or controlled mocks.
- [ ] Apple and Google provider sign-in are exercised on device.
- [ ] Profile Basics GET/PATCH is exercised against staging.
- [ ] Returning-user onboarding does not flash or restart completed steps.
- [ ] Camera/avatar upload works on device.
- [ ] Contacts discovery and prior-request flow work on device with test accounts.

The first two are repository merge checks. The remaining device/integration checks are release-readiness checks and may remain Pending when this implementation PR merges, but they must not be labeled Verified until performed.
